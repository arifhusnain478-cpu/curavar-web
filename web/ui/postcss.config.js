// Intentionally empty. Our styles are plain CSS (see src/theme.css) with no
// PostCSS plugins. This file exists so PostCSS resolves its config here and does
// NOT walk up the directory tree and pick up an unrelated parent-folder config.
export default { plugins: {} };
